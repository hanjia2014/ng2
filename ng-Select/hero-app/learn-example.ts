import {Component, Input, Output, EventEmitter} from '@angular/core';
import {Hero} from './hero';

@Component({
    selector: 'my-learn-example',
    template: `
        <div>{{1+1}}</div>
            `
})

export class LearnExampleComponent {
    
}